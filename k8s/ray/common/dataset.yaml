apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: ray-dataset-pvc
  labels:
    app.kubernetes.io/name: ray-dataset
    app.kubernetes.io/component: storage
    app.kubernetes.io/part-of: ray-common
spec:
  accessModes:
    - ReadWriteMany
  storageClassName: efs-ai
  resources:
    requests:
      storage: 20Gi
